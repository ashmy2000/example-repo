# Modules = Python Definition and statements for another file to us

import sys
sys.exit()

from math import sqrt, pi
print(sqrt(16))
print(pi)

# But if you have method with the same name then that will evoke not sys.exit()
# from sys import exit as bye_bye #reduces readability